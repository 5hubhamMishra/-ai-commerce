# @ai-commerce/config (reserved)

Not populated yet. This package is reserved for shared runtime configuration helpers
(feature-flag reading, environment-aware constants) once more than one app needs the same
logic — currently only `apps/api` has configuration of its own (`apps/api/src/config`), so
there's nothing to share yet. See `docs/PROJECT_PROGRESS.md` for what's built and what's next.
