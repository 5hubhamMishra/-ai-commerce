# @ai-commerce/validation (reserved)

Not populated yet. This package is reserved for request/response validation schemas shared
between `apps/web`, `apps/mobile`, and `packages/api-client` once that client package exists
(Phase 2+, once there's a real API surface for the frontends to validate against).
