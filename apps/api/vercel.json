{
  "framework": null,
  "buildCommand": "npm run config:preflight && npx prisma migrate deploy && npx prisma generate && npm run build",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/cron/:path*", "destination": "/api/cron/:path*" },
    { "source": "/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.ts": {
      "memory": 1024
    },
    "api/cron/*.ts": {
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/api/cron/process-behavioral-queue",
      "schedule": "0 0 * * *"
    }
  ]
}
